---
- name: install letsencrypt
  apt:
    name: letsencrypt
    state: latest

- name: create letsencrypt directory
  file:
    name: /var/www/letsencrypt
    state: directory

- name: Install nginx site for letsencrypt requests
  template:
    src: nginx-challenge.j2
    dest: /etc/nginx/sites-enabled/http-challenge

- name: Reload nginx to activate letsencrypt site
  service:
    name: nginx
    state: restarted

- name: Create letsencrypt certificate
  shell: letsencrypt certonly -n --webroot -w /var/www/letsencrypt -m {{ letsencrypt_email }} --agree-tos -d {{ domain_name }}
  args:
    creates: /etc/letsencrypt/live/{{ domain_name }}

- name: Generate dhparams
  shell: openssl dhparam -out /etc/nginx/new.labitat.dk.pem 2048
  args:
    creates: /etc/nginx/new.labitat.dk.pem

- name: Install nginx site for specified site
  template:
    src: "{{ domain_name }}.conf.j2"
    dest: /etc/nginx/sites-enabled/{{ domain_name }}.conf

- name: Reload nginx to activate specified site
  service: name=nginx state=restarted

- name: Add letsencrypt cronjob for cert renewal
  cron:
    name: letsencrypt_renewal
    special_time: weekly
    job: letsencrypt --renew-by-default certonly -n --webroot -w /var/www/letsencrypt -m {{ letsencrypt_email }} --agree-tos -d {{ domain_name }} && service nginx reload
